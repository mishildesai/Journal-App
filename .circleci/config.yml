version: 2.1
jobs:
  test:
    docker:
      - image: 'cirrusci/flutter'
    steps:
      - checkout
      - run: "flutter pub get"
      - run: "apt install -y libsqlite3-dev"
      - run: "flutter test"
workflows:
  test:
    jobs:
      - test
